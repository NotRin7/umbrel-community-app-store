version: '3.7'

services:
  litecoin:
    image: litecoinproject/litecoin-core:latest
    container_name: notrin-litecoin
    restart: unless-stopped
    command:
      - -printtoconsole
      - -regtest=1
      - -server=1
      - -rpcuser=testuser
      - -rpcpassword=testpass
      - -rpcport=9332
      - -txindex=1
      - -datadir=/litecoin/.litecoin
    ports:
      - "9332:9332"
      - "9333:9333"
